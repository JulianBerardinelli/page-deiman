---
import Button from './Button.astro';
---

<section class="content-carousel grid min-h-[65vh] grid-cols-1 gap-0 overflow-hidden md:grid-cols-3">
  <div
    class="carousel-panel relative flex items-center justify-center bg-cover bg-center px-6 py-12 text-center text-white"
    style="background-image: url('/images/backgrounds/industriales.jpg');"
  >
    <div class="absolute inset-0 bg-black/40"></div>
    <div class="carousel-panel__content relative z-10 max-w-xs space-y-4">
      <h2 class="text-2xl font-semibold uppercase tracking-wide">Industriales</h2>
      <p class="text-sm text-white/90">
        Soluciones de sabores a escala, pensadas para procesos industriales y grandes volúmenes.
      </p>
      <Button variant="primary" href="/industriales" class="inline-block">
        Descubrir
      </Button>
    </div>
  </div>

  <div
    class="carousel-panel relative flex items-center justify-center bg-cover bg-center px-6 py-12 text-center text-white"
    style="background-image: url('/images/backgrounds/semi-industriales.jpg');"
  >
    <div class="absolute inset-0 bg-black/40"></div>
    <div class="carousel-panel__content relative z-10 max-w-xs space-y-4">
      <h2 class="text-2xl font-semibold uppercase tracking-wide">Semi industriales</h2>
      <p class="text-sm text-white/90">
        Alternativas flexibles para producciones medianas con foco en rendimiento y sabor.
      </p>
      <Button variant="primary" href="/semi-industriales" class="inline-block">
        Ver más
      </Button>
    </div>
  </div>

  <div
    class="carousel-panel relative flex items-center justify-center bg-cover bg-center px-6 py-12 text-center text-white"
    style="background-image: url('/images/backgrounds/hogar.jpg');"
  >
    <div class="absolute inset-0 bg-black/40"></div>
    <div class="carousel-panel__content relative z-10 max-w-xs space-y-4">
      <h2 class="text-2xl font-semibold uppercase tracking-wide">Hogar</h2>
      <p class="text-sm text-white/90">
        Sabores ideales para aplicaciones caseras y emprendimientos de pequeña escala.
      </p>
      <Button variant="primary" href="/hogar" class="inline-block">
        Explorar
      </Button>
    </div>
  </div>
</section>

<style>
  .content-carousel .carousel-panel {
    position: relative;
  }

  .content-carousel .carousel-panel::before {
    content: '';
    position: absolute;
    top: 0;
    right: -40px;
    height: 100%;
    width: 80px;
    background: rgba(255, 255, 255, 0.2);
    transform: skewX(-12deg);
    z-index: 2;
  }

  .content-carousel .carousel-panel:last-child::before {
    display: none;
  }

  .content-carousel .carousel-panel__content {
    animation: panel-reveal 0.9s ease-out both;
  }

  .content-carousel .carousel-panel:nth-child(2) .carousel-panel__content {
    animation-delay: 0.12s;
  }

  .content-carousel .carousel-panel:nth-child(3) .carousel-panel__content {
    animation-delay: 0.24s;
  }

  @keyframes panel-reveal {
    from {
      opacity: 0;
      transform: translateY(24px);
      filter: blur(8px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
      filter: blur(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .content-carousel .carousel-panel__content {
      animation: none;
    }
  }

  @media (max-width: 767px) {
    .content-carousel .carousel-panel::before {
      display: none;
    }
  }
</style>
